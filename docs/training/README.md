# 교육자료

**화면이 바뀌면 여기도 바뀌어야 합니다.** 그래서 손으로 만들지 않고 스크립트로 만듭니다.

## 다시 만드는 법

```bash
python tools/capture_screens.py        # 서버를 띄워 16종 × 2모드 + 대시보드 화면 캡처
python tools/prep_slide_images.py      # 슬라이드에 넣을 크기로 자르기
node   tools/deck_overview.js          # ① 전체 개요 덱
node   tools/deck_products.js          # ②~⑤ 13·14·15·16번 덱
python tools/qa_deck.py docs/training/*.pptx   # 밖으로 나간 것·겹친 것·넘칠 글
```

## 나오는 것

| 파일 | 장수 | 내용 |
|---|---|---|
| `교육자료_통합관리자대시보드.pptx` | 14 | 전체 개요·16종 목록·두 모드 개념·권한환경·정기실행 |
| `교육자료_13_공인중개사기출.pptx` | 10 | 파는 것 → 설치 → 관리자 → 클라이언트 → 사례 → FAQ → 조심할 것 |
| `교육자료_14_시니어영상.pptx` | 10 | 위와 같은 구성 |
| `교육자료_15_네이버블로그.pptx` | 10 | 위와 같은 구성 |
| `교육자료_16_해외연사초청.pptx` | 10 | 위와 같은 구성 |

pptx 와 캡처 png 는 용량이 커서 **커밋하지 않습니다**(`.gitignore`). 위 명령으로 언제든 다시 만듭니다.

## 점검

- `tools/qa_deck.py` — 좌표와 글자 수로 봅니다. 슬라이드 밖으로 나간 것, 글끼리 겹친 것, 상자를 넘칠 것 같은 글
- **이 환경에서는 LibreOffice 가 파일을 못 열어**(텍스트 파일조차 실패) 그림으로 뽑아 눈으로 보는 점검을 못 했습니다.
  PowerPoint 가 있는 컴퓨터에서 한 번 열어 보시고, 어긋난 곳이 있으면 알려 주세요

## 나머지 12종 덱

우선 4종만 만들었습니다. 17개 덱은 오히려 안 읽히기 때문입니다.
필요하시면 `tools/deck_products.js` 에 같은 모양으로 항목을 추가하면 됩니다.
뼈대(`buildDeck`)는 그대로 쓰고 내용만 채우면 됩니다.
