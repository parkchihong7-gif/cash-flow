# 비밀키 — 절대 커밋 금지 (CLAUDE.md §6)
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
token.json
client_secret*.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
*.egg-info/

# Node / TypeScript
node_modules/
dist/
build/
.next/

# DB · 로컬 데이터 — dashboard.db 에 고객 명단이 있다. 직접 백업할 것 (docs/admin-manual.md 8장)
*.sqlite
*.sqlite3
*.db
data/

# 대시보드 파일 편집기가 남기는 백업
*.bak

# 산출물 (생성물은 저장소에 담지 않는다)
outputs/
renders/
*.mp4
*.wav

# OS · 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/

# 대시보드 세션 서명용 비밀값. 절대 커밋하지 않는다.
.dashboard_secret

# 정적 스냅샷 결과 (python -m dashboard.snapshot)
dashboard-snapshot/
dashboard-snapshot.zip

# 교육자료용 화면 캡처 (용량이 커서 커밋하지 않는다. tools/capture_screens.py 로 다시 만든다)
docs/training/screens/

# 교육자료 pptx (용량. tools/deck_*.js 로 다시 만든다)
docs/training/*.pptx
docs/training/*.pdf
