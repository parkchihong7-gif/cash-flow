# 공통
anthropic>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
PyYAML>=6.0
jinja2>=3.1.0
requests>=2.31.0

# 통합 관리자 대시보드
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
markdown>=3.5

# 11번 자동화 대행 납품 키트
#   pandas      주간 보고서 집계 (숫자는 여기서만 나옵니다. Claude 는 해석만 씁니다)
#   APScheduler 인스타 예약 발행 — 1분마다 큐를 봅니다
pandas>=2.0.0
APScheduler>=3.10.0

# 문서 산출
python-docx>=1.1.0
python-pptx>=1.0.0
openpyxl>=3.1.0

# 외부 API
google-api-python-client>=2.120.0
google-auth>=2.28.0

# 테스트
pytest>=8.0.0
httpx>=0.27.0

# 공구 정산 장부(products/groupbuy-ledger)가 만든 엑셀을 다시 계산해 검산하는 데 씁니다.
# 테스트가 "엑셀이 계산한 값 == 파이썬이 따로 계산한 값" 을 이걸로 확인합니다.
# LibreOffice 가 깔려 있으면 그쪽을 먼저 씁니다.
formulas>=1.2.0
