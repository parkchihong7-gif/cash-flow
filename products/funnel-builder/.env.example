# 복사해서 .env 로 쓴다. 저장소 루트의 .env 를 먼저 읽으므로 보통은 루트 것만 있으면 된다.
ANTHROPIC_API_KEY=sk-ant-...

# 기본 모델. --model 로도 덮어쓸 수 있다.
CLAUDE_MODEL=claude-sonnet-4-6

# AI 생성물 표시 기본값. 끄려면 0 (권장하지 않음 — 인공지능기본법 제31조)
AI_LABEL=1
