{
  "id": "webhook-trigger",
  "label": "웹훅 트리거",
  "category": "trigger",
  "description": "외부에서 HTTP 요청이 오면 시작합니다. 폼 제출·결제 알림 등에 씁니다.",
  "n8n_type": "n8n-nodes-base.webhook",
  "type_version": 2,
  "is_trigger": true,
  "credential": null,
  "params": [
    {
      "key": "path",
      "label": "웹훅 경로",
      "type": "string",
      "default": "incoming"
    },
    {
      "key": "method",
      "label": "HTTP 메서드",
      "type": "string",
      "default": "POST"
    }
  ],
  "parameters": {
    "httpMethod": "{{method}}",
    "path": "{{path}}",
    "responseMode": "onReceived",
    "options": {}
  },
  "outputs": [
    "main"
  ],
  "inputs": 0
}
