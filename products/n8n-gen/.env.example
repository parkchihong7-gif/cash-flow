# 복사해서 .env 로 쓴다. 저장소 루트의 .env 를 먼저 읽으므로 보통은 루트 것만 있으면 된다.
ANTHROPIC_API_KEY=sk-ant-...
CLAUDE_MODEL=claude-sonnet-4-6
AI_LABEL=1

# --push 를 쓸 때만 필요하다. n8n 화면 Settings → n8n API 에서 키를 만든다.
N8N_URL=http://localhost:5678
N8N_API_KEY=
